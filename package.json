{
  "name": "liverush",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm --filter user-app dev",
    "dev:user": "pnpm --filter user-app dev",
    "dev:studio": "pnpm --filter studio dev",
    "build": "pnpm --filter user-app build",
    "build:user": "pnpm --filter user-app build",
    "build:studio": "pnpm --filter studio build",
    "preview:user": "pnpm --filter user-app preview",
    "lint": "pnpm -r --if-present lint"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
