// Thin shim — real implementation in @liverush/ui. Keeps existing
// `import { Button } from "@/components/ui/button"` call sites working.
export { Button, buttonVariants, type ButtonProps } from "@liverush/ui";
