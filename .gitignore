# OS
.DS_Store

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Vite build output
dist
dist-ssr
*.local
.vite/

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
.env.*.local

# Vercel
.vercel

# Claude Code local settings (per-user permissions)
.claude/settings.local.json

# Supabase CLI per-machine state (project ref cache, etc.) — never
# useful to share across the team. `**/` so it ignores both the
# root-level `supabase/.temp/` and the per-app one inside
# `apps/user-app/supabase/.temp/`.
**/supabase/.temp/

# TypeScript incremental build cache
*.tsbuildinfo
